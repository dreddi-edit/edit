{
  "name": "site-editor-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix server install && npm --prefix dashboard install && npm --prefix dashboard run build",
    "start": "cd server && node index.js",
    "dev": "npm --prefix server run dev"
  }
}
