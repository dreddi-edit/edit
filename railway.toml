[build]
  builder = "nixpacks"

[deploy]
  startCommand = "npm start"
  port = 8080
  healthcheckPath = "/health"
  healthcheckTimeout = 100
  restartPolicyType = "on_failure"

[env]
  PORT = "8080"
  NODE_ENV = "production"
