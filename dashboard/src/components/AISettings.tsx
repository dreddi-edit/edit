import React from 'react';

export function AISettings() {
  return (
    <div className="rounded-lg border border-slate-700 bg-slate-950/60 p-4 text-sm text-slate-300">
      AI quick-test actions were removed from this surface because they were demo-only and not backed by a real product flow.
    </div>
  );
}

export default AISettings;
