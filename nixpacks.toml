[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = [
  "cd server && npm install",
  "cd dashboard && npm install"
]

[phases.build]
cmds = [
  "cd dashboard && npm run build"
]

[start]
cmd = "node server/index.js"
