import React from 'react';

export function LegalGenerator() {
  return (
    <div className="rounded-lg border border-slate-700 bg-slate-950/60 p-4 text-sm text-slate-300">
      Legal document generation is not active in this build yet. The previous version used demo-only behavior and was removed from the live UI.
    </div>
  );
}

export default LegalGenerator;
