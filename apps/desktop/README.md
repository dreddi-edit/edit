# Desktop App

Desktop app area (Electron or Tauri) is intentionally separated from web and mobile.

Suggested structure:

- `apps/desktop/src` for desktop shell code
- `apps/desktop/resources` for icons and installer assets
- `apps/desktop/build` for packaging config

Current status: placeholder, ready for implementation.
