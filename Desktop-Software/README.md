# Desktop App

Desktop app area (Electron or Tauri) is intentionally separated from web and mobile.

Suggested structure:

- `Desktop-Software/src` for desktop shell code
- `Desktop-Software/resources` for icons and installer assets
- `Desktop-Software/build` for packaging config

Current status: placeholder, ready for implementation.
